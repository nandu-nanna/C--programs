#include<stdio.h>
void main()
{
    int i,n,sum,rem,rev;
    printf("n");
    scanf("%d",&n);
    rev=0;
    while(n>0)
    {
        rem=n%10;
        rev=rev*10+rem;
        n=n/10;
    }
    printf("the reverse number %d",rev);
}
